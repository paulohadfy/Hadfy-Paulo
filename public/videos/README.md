# Drop your video here

Place `Blandat.mov` in this directory (`public/videos/Blandat.mov`) before running the Remotion studio or render command.

The path is already wired into the composition via `constants.ts`.
